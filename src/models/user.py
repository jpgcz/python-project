class User:
    """Represents a User.

        Attributes:
            name (str)
            age (int)
            is_active (bool)
    """
    def __init__(self, name, age, is_active):
        self.name = name
        self.age = age
        self.is_active = is_active

